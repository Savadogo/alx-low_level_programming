#include "main.h"
/**
 * mul - Returns multiplication of a * b
 * Return: a * b
 * @a: first number to use for multiplication
 * @b: second number to use for multiplication
 */

int mul(int a, int b)
{
	return (a * b);
}

