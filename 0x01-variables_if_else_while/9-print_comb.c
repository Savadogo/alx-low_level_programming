#include <stdlib.h>
#include <stdio.h>
#include <ctype.h>
/**
 * main - Print sign of number
 * Return: 0
 */
int main(void)
{
	int i, j;

	for (i = 0; i < 10; i++)
	{
		putchar('0' + i);
		putchar(',');
		putchar(' ');
	}
	return (0);
}
