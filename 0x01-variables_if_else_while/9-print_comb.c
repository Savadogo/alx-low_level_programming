#include <stdlib.h>
#include <stdio.h>
#include <ctype.h>
/**
 * main - Print sign of number
 * Return: 0
 */
int main(void)
{
	int i, j;

	for (i = 0; i < 10; i++)
	{
		for (j = 0; j < 10; j++)
		{
			putchar('0' + i);
			putchar('0' + j);
			putchar(',');
			putchar(' ');
		}
	}
	return (0);
}
